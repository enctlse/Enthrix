rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ВРЕМЕННО: Полный доступ для тестирования
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
